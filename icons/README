tlineIcon.xpm is the master artwork, the others are exported via gimp.

Note that XPM files generated by gimp are missing a "const" declaration that
C++ needs, and they contain a "static" declaration that we don't want.
Also, for windows, the XPM becomes a resource, so we change its name to aaaa
because some versions of windows apparently use a lexical sort to choose the
overall program icon.

Thus, we change the declaration to:

char const * aaaa_xpm[] = {

